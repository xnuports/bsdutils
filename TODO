- sed from FreeBSD is a problem, should we import from NetBSD for that
  command instead?

- Add the following things:
  - calendar

- Add POSIX ACL support using libcap to these commands, but make it a build
  time option:
  - ls
  - users

- Add SELinux support using libselinux to these commands, but make it a
  build time option:
  - cp
  - id
  - install
  - ls
  - mkdir
  - mkfifo
  - mknod
  - mv
  - stat
